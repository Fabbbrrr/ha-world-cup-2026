"""Constants for the World Cup 2026 integration."""

DOMAIN = "world_cup_2026"
DEFAULT_NAME = "World Cup 2026"

# Config entry data keys (set at initial setup, not user-editable afterwards)
CONF_API_KEY = "api_key"

# Options keys (editable via the integration's Settings → Configure screen)
OPT_DEMO_MODE = "demo_mode"

# Option defaults
DEFAULT_DEMO_MODE = False
